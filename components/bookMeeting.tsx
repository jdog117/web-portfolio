"use client";
import { Button } from "@/components/ui/button";

export default function Book() {
    return (
        <Button className="fixed bottom-0 right-0 p-6 md:px-9 md:py-6 z-10 m-8 md:m-16 rounded-full">
            Let{`'`}s talk!
        </Button>
    );
}
